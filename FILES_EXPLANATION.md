# Объяснение файлов проекта

Этот документ описывает назначение каждого файла в проекте Telegram To-Do бота.

---

## 📄 Основные файлы (обязательные)

### `main.py` - Главный файл бота
**Назначение:** Основной файл, содержащий всю логику работы бота.

**Что внутри:**
- Импорты библиотек (telegram, sqlite3, datetime и др.)
- Функции работы с базой данных SQLite:
  - `init_db()` - создание таблицы при первом запуске
  - `load_tasks()` - загрузка всех задач из базы
  - `insert_task()` - добавление новой задачи
  - `update_task()` - обновление задачи (например, отметка как выполненной)
  - `delete_task_by_id()` - удаление задачи
- Функции обработки команд:
  - `start()` - команда /start
  - `help_command()` - команда /help
  - `add_task()` - команда /add_task
  - `list_tasks()` - команда /list_tasks
  - `complete_task()` - команда /complete_task
  - `delete_task()` - команда /delete_task
- Обработчики сообщений и callback-кнопок
- Функция `main()` - запуск бота

**Когда используется:** Всегда, когда бот работает.

---

### `requirements.txt` - Зависимости проекта
**Назначение:** Список всех Python-библиотек, необходимых для работы бота.

**Что внутри:**
```
python-telegram-bot>=22.5  # Библиотека для работы с Telegram Bot API
python-dotenv>=1.2.0       # Для загрузки переменных окружения из .env файла
```

**Когда используется:** 
- При первой установке проекта: `pip install -r requirements.txt`
- При развертывании на сервере (Railway, Heroku и т.д.)

**Важно:** SQLite (`sqlite3`) входит в стандартную библиотеку Python, поэтому его не нужно устанавливать отдельно.

---

### `tasks.db` - База данных SQLite
**Назначение:** Файл базы данных, где хранятся все задачи.

**Что внутри:**
- Таблица `tasks` с полями:
  - `id` - уникальный идентификатор (автоинкремент)
  - `task` - описание задачи
  - `deadline` - дедлайн (дата в формате ДД.ММ.ГГГГ)
  - `employee` - сотрудник (username или имя)
  - `completed` - статус выполнения (0 = не выполнено, 1 = выполнено)
  - `created_at` - дата и время создания

**Когда используется:** 
- Автоматически создается при первом запуске бота
- Обновляется при каждом добавлении/изменении/удалении задачи

**Важно:** 
- Этот файл НЕ должен попадать в Git (уже добавлен в `.gitignore`)
- На Railway этот файл работает как обычный локальный файл
- При удалении файла все данные будут потеряны!

---

### `.env` - Конфигурация (не в Git)
**Назначение:** Хранит секретные данные (токен бота).

**Что внутри:**
```
BOT_TOKEN=ваш_токен_от_BotFather
```

**Когда используется:** При каждом запуске бота для загрузки токена.

**Важно:** 
- Этот файл НЕ должен попадать в Git (уже добавлен в `.gitignore`)
- Создается вручную пользователем
- НЕ коммитится в репозиторий по соображениям безопасности

---

### `.gitignore` - Исключения для Git
**Назначение:** Указывает Git, какие файлы НЕ нужно коммитить в репозиторий.

**Что внутри:**
- `.env` - файл с токеном (секретные данные)
- `tasks.db` - база данных (рабочий файл)
- `__pycache__/` - кэш Python (автоматически генерируется)
- `venv/`, `env/` - виртуальные окружения
- `.vscode/`, `.idea/` - настройки IDE

**Когда используется:** Автоматически Git при каждом коммите.

**Важно:** Защищает от случайной загрузки секретных данных в публичный репозиторий.

---

## 📚 Документация (опциональные файлы)

### `README.md` - Главная документация
**Назначение:** Основная документация проекта для других разработчиков и пользователей.

**Что внутри:**
- Описание проекта
- Инструкции по установке
- Инструкции по настройке
- Примеры использования
- Список команд бота

**Когда используется:** 
- При первом знакомстве с проектом
- При развертывании на новом сервере
- Для других разработчиков

---

### `TESTING.md` - Инструкция по тестированию
**Назначение:** Подробная инструкция, как протестировать работу бота.

**Что внутри:**
- Пошаговые инструкции по тестированию
- Команды для проверки базы данных
- Чек-лист тестирования
- Решение возможных проблем

**Когда используется:** 
- После внесения изменений в код
- При проверке работоспособности бота
- При отладке проблем

---

### `FILES_EXPLANATION.md` - Этот файл
**Назначение:** Объяснение назначения всех файлов проекта.

**Когда используется:** Когда нужно понять структуру проекта.

---

## 🧪 Тестовые файлы (опциональные)

### `test_db.py` - Тестовый скрипт базы данных
**Назначение:** Автоматическое тестирование всех функций работы с базой данных.

**Что внутри:**
- Тест создания таблицы
- Тест добавления задач
- Тест загрузки задач
- Тест обновления задач
- Тест удаления задач
- Тест структуры таблицы

**Когда используется:** 
- После изменения функций базы данных
- Для проверки работоспособности перед деплоем
- При отладке проблем с базой данных

**Как запустить:**
```bash
python test_db.py
```

**Важно:** Этот файл можно удалить, если не нужен, но он полезен для тестирования.

---

## 📁 Структура проекта (итого)

```
tgBot/
├── main.py                 # ⭐ Главный файл бота (ОБЯЗАТЕЛЬНО)
├── requirements.txt        # ⭐ Зависимости (ОБЯЗАТЕЛЬНО)
├── tasks.db               # ⭐ База данных (создается автоматически)
├── .env                   # ⭐ Конфигурация (создается вручную)
├── .gitignore             # ⭐ Исключения для Git (ОБЯЗАТЕЛЬНО)
│
├── README.md              # 📚 Документация (рекомендуется)
├── TESTING.md             # 📚 Инструкция по тестированию (опционально)
├── FILES_EXPLANATION.md   # 📚 Этот файл (опционально)
│
└── test_db.py             # 🧪 Тестовый скрипт (опционально)
```

---

## 🔄 Жизненный цикл файлов

### При первом запуске:
1. Создается `tasks.db` (автоматически)
2. Создается таблица `tasks` (автоматически)
3. Бот начинает работать

### При работе бота:
- `main.py` - выполняется постоянно
- `tasks.db` - обновляется при каждом изменении задач
- `.env` - читается при запуске

### При деплое на сервер:
- `requirements.txt` - используется для установки зависимостей
- `main.py` - запускается как основной процесс
- `tasks.db` - создается автоматически на сервере

---

## ❓ Часто задаваемые вопросы

**Q: Можно ли удалить `test_db.py`?**  
A: Да, это опциональный файл. Но он полезен для тестирования.

**Q: Можно ли удалить `TESTING.md`?**  
A: Да, это опциональный файл. Но он полезен для документирования.

**Q: Что будет, если удалить `tasks.db`?**  
A: Все задачи будут потеряны. Бот создаст новый пустой файл при следующем запуске.

**Q: Нужно ли коммитить `tasks.db` в Git?**  
A: Нет! Этот файл уже в `.gitignore`. База данных - это рабочий файл, его не нужно версионировать.

**Q: Что делать, если потерял `.env`?**  
A: Создайте новый файл `.env` с токеном от BotFather:
```
BOT_TOKEN=ваш_токен_здесь
```

---

**Готово!** Теперь вы знаете назначение каждого файла в проекте! 🎉

