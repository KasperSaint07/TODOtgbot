# Инструкция по тестированию бота

## Шаг 1: Тестирование базы данных

Запустите тестовый скрипт для проверки всех функций базы данных:

```bash
python test_db.py
```

Этот скрипт проверит:
- ✅ Создание таблицы
- ✅ Добавление задач
- ✅ Загрузку задач
- ✅ Обновление задач
- ✅ Удаление задач
- ✅ Структуру таблицы

**Ожидаемый результат:** Все тесты должны пройти успешно.

---

## Шаг 2: Миграция данных из JSON (опционально)

Если у вас есть данные в `tasks.json`, мигрируйте их в SQLite:

```bash
python migrate_json_to_sqlite.py
```

Этот скрипт:
- Загрузит все задачи из `tasks.json`
- Перенесет их в `tasks.db`
- Сохранит все поля (id, task, deadline, employee, completed, created_at)

**Примечание:** После миграции можно удалить `tasks.json` (но лучше оставить как резервную копию).

---

## Шаг 3: Тестирование через Telegram бота

### 3.1. Запустите бота

```bash
python main.py
```

Должно появиться сообщение: `Бот запущен и готов к работе!`

### 3.2. Тестовые команды в Telegram

Откройте ваш бот в Telegram и выполните следующие тесты:

#### Тест 1: Главное меню
```
/start
```
**Ожидаемый результат:** Появится меню с кнопками.

#### Тест 2: Добавление задачи
Нажмите кнопку **"➕ Добавить задание"** или отправьте:
```
Задание: Тестовая задача
Дедлайн: 31.12.2024
Сотрудник: @test_user
```

**Ожидаемый результат:** 
- Сообщение "Задание добавлено!"
- Задача появится в базе данных

#### Тест 3: Просмотр всех задач
Нажмите кнопку **"📋 Все задания"** или отправьте:
```
/list_tasks
```

**Ожидаемый результат:** 
- Список всех задач с кнопками управления
- Каждая задача показывает: ID, описание, дедлайн, сотрудника, статус

#### Тест 4: Фильтрация задач
Проверьте кнопки:
- **"🟢 Активные"** - только невыполненные
- **"✅ Выполненные"** - только выполненные
- **"⏰ Просроченные"** - только просроченные

**Ожидаемый результат:** Каждый фильтр показывает правильные задачи.

#### Тест 5: Выполнение задачи
1. Откройте список задач
2. Нажмите кнопку **"✅ Выполнить"** у любой задачи

**Ожидаемый результат:** 
- Задача помечается как выполненная
- Статус меняется на "✅ Completed"
- Кнопка "✅ Выполнить" исчезает

#### Тест 6: Удаление задачи
1. Откройте список задач
2. Нажмите кнопку **"🗑️ Удалить"** у любой задачи

**Ожидаемый результат:** 
- Задача удаляется из списка
- Сообщение "Задание #X удалено!"

#### Тест 7: Команды через /complete_task и /delete_task
```
/complete_task 1
/delete_task 2
```

**Ожидаемый результат:** Команды работают корректно.

---

## Шаг 4: Проверка файла базы данных

После тестирования проверьте, что файл `tasks.db` создан:

```bash
# Windows PowerShell
dir tasks.db

# Linux/Mac
ls -lh tasks.db
```

**Ожидаемый результат:** Файл `tasks.db` существует и имеет размер > 0 байт.

---

## Шаг 5: Проверка содержимого базы данных (опционально)

Можно проверить содержимое базы данных напрямую:

```bash
python -c "import sqlite3; conn = sqlite3.connect('tasks.db'); cursor = conn.cursor(); cursor.execute('SELECT * FROM tasks'); print(cursor.fetchall()); conn.close()"
```

Или используйте любой SQLite браузер (например, DB Browser for SQLite).

---

## Возможные проблемы и решения

### Проблема: "Ошибка! Токен бота не найден"
**Решение:** Создайте файл `.env` с содержимым:
```
BOT_TOKEN=ваш_токен_здесь
```

### Проблема: База данных не создается
**Решение:** 
- Проверьте права на запись в директории
- Убедитесь, что `init_db()` вызывается при запуске

### Проблема: Задачи не сохраняются
**Решение:**
- Проверьте, что `tasks.db` создан
- Запустите `test_db.py` для диагностики
- Проверьте логи ошибок в консоли

### Проблема: На Railway база данных не работает
**Решение:**
- SQLite работает с локальными файлами на Railway
- Убедитесь, что путь к файлу относительный: `tasks.db`
- Проверьте, что файл не в `.gitignore` (если нужно сохранять данные)

---

## Чек-лист тестирования

- [ ] Тест базы данных (`test_db.py`) прошел успешно
- [ ] Бот запускается без ошибок
- [ ] Команда `/start` работает
- [ ] Добавление задачи работает
- [ ] Просмотр всех задач работает
- [ ] Фильтры (активные/выполненные/просроченные) работают
- [ ] Выполнение задачи через кнопку работает
- [ ] Удаление задачи через кнопку работает
- [ ] Команды `/complete_task` и `/delete_task` работают
- [ ] Файл `tasks.db` создан и содержит данные
- [ ] Все функции работают так же, как с JSON

---

**Готово!** Если все тесты пройдены, бот готов к использованию! 🎉

